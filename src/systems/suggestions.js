const { SlashCommandBuilder, PermissionFlagsBits, ActionRowBuilder, ButtonBuilder, ButtonStyle } = require('discord.js');
const {admin}=require('../utils/helpers');
module.exports={data:new SlashCommandBuilder().setName('suggest').setDescription('Crea una sugerencia').addStringOption(o=>o.setName('text').setDescription('Sugerencia').setRequired(true)),async execute(i,{db}){const cfg=db.get(i.guildId,'suggestions',{});const ch=cfg.channel?i.guild.channels.cache.get(cfg.channel):i.channel;const m=await ch.send({content:`💡 **Sugerencia de ${i.user}:**\n${i.options.getString('text')}`,components:[new ActionRowBuilder().addComponents(new ButtonBuilder().setCustomId('suggest:up').setLabel('Aprobar').setStyle(ButtonStyle.Success),new ButtonBuilder().setCustomId('suggest:down').setLabel('Rechazar').setStyle(ButtonStyle.Danger))]});await m.react('👍');await m.react('👎');db.log(i.guildId,'suggestions','created',{message:m.id,user:i.user.id});return i.reply({content:'Sugerencia publicada.',ephemeral:true});}};
async function button(i,db){await i.reply({content:'Voto registrado.',ephemeral:true});db.log(i.guildId,'suggestions','vote',{user:i.user.id,choice:i.customId});}module.exports.button=button;
