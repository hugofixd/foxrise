const { SlashCommandBuilder, PermissionFlagsBits, ChannelType, EmbedBuilder, ActionRowBuilder, ButtonBuilder, ButtonStyle } = require('discord.js');
const {admin}=require('../utils/helpers');
module.exports={data:new SlashCommandBuilder().setName('ticket').setDescription('Sistema de tickets FOXRISE').addSubcommand(s=>s.setName('setup').setDescription('Configura categoría y canal de logs').addChannelOption(o=>o.setName('category').setDescription('Categoría').setRequired(true)).addChannelOption(o=>o.setName('logs').setDescription('Logs').setRequired(true))).addSubcommand(s=>s.setName('panel').setDescription('Publica panel').addStringOption(o=>o.setName('title').setDescription('Título').setRequired(true)).addStringOption(o=>o.setName('description').setDescription('Descripción').setRequired(true))).addSubcommand(s=>s.setName('stats').setDescription('Estadísticas')),async execute(i,{db})=>{if(!admin(i))return i.reply({content:'Sin permisos.',ephemeral:true});const s=i.options.getSubcommand();if(s==='setup'){db.set(i.guildId,'tickets',{category:i.options.getChannel('category').id,logs:i.options.getChannel('logs').id});return i.reply('Sistema de tickets configurado.');}if(s==='panel'){const e=new EmbedBuilder().setColor(0xf97316).setTitle(i.options.getString('title')).setDescription(i.options.getString('description'));const row=new ActionRowBuilder().addComponents(new ButtonBuilder().setCustomId('ticket:create').setLabel('Abrir ticket').setEmoji('🎫').setStyle(ButtonStyle.Primary));await i.channel.send({embeds:[e],components:[row]});return i.reply({content:'Panel publicado.',ephemeral:true});}const n=db.db.prepare('SELECT COUNT(*) n FROM tickets WHERE guild_id=?').get(i.guildId).n;return i.reply(`FOXRISE ha registrado ${n} tickets.`);}};
