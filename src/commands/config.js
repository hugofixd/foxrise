const { SlashCommandBuilder, PermissionFlagsBits } = require('discord.js');
const {admin}=require('../utils/helpers');
module.exports={data:new SlashCommandBuilder().setName('config').setDescription('Configura FOXRISE por servidor').setDefaultMemberPermissions(PermissionFlagsBits.ManageGuild).addSubcommand(s=>s.setName('view').setDescription('Ver configuración')).addSubcommand(s=>s.setName('reset').setDescription('Borrar configuración')).addSubcommand(s=>s.setName('set').setDescription('Establecer un ajuste').addStringOption(o=>o.setName('key').setDescription('Clave').setRequired(true)).addStringOption(o=>o.setName('value').setDescription('JSON o texto').setRequired(true))),async execute(i,{db})=>{if(!admin(i))return i.reply({content:'Necesitas gestionar el servidor.',ephemeral:true});const sub=i.options.getSubcommand();if(sub==='view'){return i.reply({content:'Usa las subconfiguraciones de cada sistema; la base de datos está activa.',ephemeral:true});}if(sub==='reset'){db.db.prepare('DELETE FROM guild_config WHERE guild_id=?').run(i.guildId);return i.reply('Configuración de FOXRISE reiniciada.');}let v=i.options.getString('value');try{v=JSON.parse(v)}catch{}db.set(i.guildId,i.options.getString('key'),v);return i.reply(`Configuración [1m${i.options.getString('key')}[0m guardada.`)}};
